# -*- coding: utf-8 -*-
# vim: ft=yaml
---
elasticsearch:
  enabled: False
  dep_pkgs:
    - ca-certificates-java
    - openjdk-8-jre-headless
  dep_fromrepo: jessie-backports
  pkg: elasticsearch
  service:
    name: elasticsearch
    state: dead
    enable: False
  xpack:
    log4j2: {}
